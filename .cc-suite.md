# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/cc-suite:init`. Edit freely — all fields are optional.

## Project

- **Stack**: Markdown + YAML book project; Python 3.10+ validators inherited from the parent workshop (`<workshop-root>/scripts/`)
- **Test command**: `python3 scripts/validate_book.py books/responsibility_laundering --strict-placeholders` (run from the workshop root); no JS/TS test runner
- **Source directories**: `book/`, `.claude/`, `dev-docs/`

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.5
- **Default effort**: high
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

quality-first

Additional instructions appended to every audit's developer-instructions:

```text
Prioritize code quality findings. Flag untested critical paths, high cyclomatic complexity, and DRY violations as High regardless of other severity heuristics. For prose, YAML cards, and audit reports under book/ and .claude/, treat the equivalent quality bar as: duplicated case material across case files, contradiction with the taxonomy in .claude/rules/01-case-taxonomy.md, evidence grade missing or unjustified per .claude/rules/02-evidence-grades.md, and missing handoff fields per .claude/rules/00-five-values.md (rule 5).
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
.git/
.claude/logs/
.claude/state/
dev-docs/01_BOOK_PREPARATION_DECKS/original_zip/
dev-docs/02_RESEARCH_PACKAGES/original_zip/
dev-docs/03_CLAUDE_AI_CREW_WORKSPACE/original_zip/
dev-docs/04_ARCHIVE_SUPERSEDED_OR_REFERENCE/original_zip/
dev-docs/03_CLAUDE_AI_CREW_WORKSPACE/extracted/
dev-docs/04_ARCHIVE_SUPERSEDED_OR_REFERENCE/extracted/
**/*.pptx
**/*.png
**/*.zip
**/.DS_Store
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.
Use this to tell Codex about your project's conventions, architecture, or constraints.

```text
This is the "No One Did It" responsibility-laundering book project, governed
by `CLAUDE.md` at this project root and inheriting workshop rules from
`<workshop-root>/AGENTS.md`. Treat
`dev-docs/` as read-only relocated bundle archive (decks, research packages,
master case matrix, source ledger, workspace snapshots). The active workspace
is at the project root: `CLAUDE.md`, `README.md`, `.claude/`, `book/`.

When auditing or refactoring, honor:
- the Global Five Over-Rules in `.claude/rules/00-five-values.md`
- the case taxonomy in `.claude/rules/01-case-taxonomy.md` (pure / partial / system-or-object alibi / cost-bearing)
- evidence grades A/B/C/D in `.claude/rules/02-evidence-grades.md`
- the no-overlap role map in `.claude/rules/03-no-overlap-role-map.md`
- the style guide in `.claude/rules/04-style-guide.md`

Do not propose changes inside `dev-docs/01_…`, `dev-docs/02_…`, or the
extracted snapshots under `dev-docs/03_…/extracted/` or
`dev-docs/04_…/extracted/`. Those are integrity-checked by
`dev-docs/00_START_HERE/checksums_sha256.txt`.
```
