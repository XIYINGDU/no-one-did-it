# Agent 记忆

此目录存储每个 agent 的独立记忆文件。每个 agent 有独立子目录，
包含 `MEMORY.md` 索引和具体记忆文件。

## 记忆类型

- **`project_*`** — 项目级决策和知识（如排序决策、设计原理）
- **`feedback_*`** — agent 收到的纠正反馈（如「术语归因错误」）

## 工作原理

agent 激活时，其 `MEMORY.md` 被加载到上下文。索引链接到具体记忆文件。
当 agent 犯错时，编排者将其记录为 `feedback_*` 记忆。下次激活时记忆加载，错误不再重复。

这不是提示词优化——这是系统积累免疫记忆。
